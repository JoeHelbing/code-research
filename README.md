# code-research
For each new experiment, create a new folder.
Always use pixi for package management and running code if the language is python. Create a new pixi .venv and pixi.toml file for each project like `project-folder/pixi.toml`.
