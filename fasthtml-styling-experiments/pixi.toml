[project]
name = "fasthtml-styling-experiments"
version = "0.1.0"
description = "FastHTML Python-based styling research and experiments"
authors = ["Your Name <your.email@example.com>"]
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "win-64", "osx-arm64"]

[dependencies]
python = ">=3.11"
python-fasthtml = "*"
uvicorn = "*"
pip = "*"

[pypi-dependencies]
playwright = "*"

[tasks]
install-playwright = "playwright install chromium"
